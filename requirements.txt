# Dependencias para las funciones serverless en api/ (Vercel).
# La fuente de verdad de dependencias del proyecto es pyproject.toml;
# este archivo solo existe porque el builder Python de Vercel lo requiere.
httpx>=0.27
python-dotenv>=1.0
click>=8.1
jinja2>=3.1
